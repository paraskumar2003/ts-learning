{
  "name": "simple-ts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "tsc -w",
    "dev": "nodemon -w build -q build/index.js",
    "compile": "node_modules/.bin/concurrently -k -n COMPILER,NODEMON -c blue,green 'tsc -w' 'nodemon -w build -q build/index.js'",
    "dev2":"concurrently -k -n COMPILER,NODEMON -c yellow,blue \"tsc -w\" \"nodemon -w build -q build/index.js\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.2"
  }
}